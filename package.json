{
  "name": "handsontable - first row not validated",
  "private": true,
  "version": "0.1.0",
  "description": "handsontable - first row not validated",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "check-types": "tsc --noEmit --skipLibCheck",
    "lint": "eslint . --ext .ts",
    "test": "node -e 'console.log(\"No tests found\")'"
  },
  "dependencies": {
    "handsontable": "12.3.2",
    "parcel-bundler": "^1.6.1"
  },
  "devDependencies": {
    "typescript": "^4.2.3"
  },
  "resolutions": {
    "@babel/preset-env": "7.13.8"
  },
  "keywords": []
}
